upstream docker-frontend {
    server frontend:80;
}

server {
    listen 80;
    listen [::]:80;

    server_name bbards.com www.bbards.com;
    # index index.html index.htm;

    location / {
        # root /var/www/bbards.com;
        # index index.html index.htm;
        # try_files $uri $uri/ =404;
        include /etc/nginx/proxy_headers.conf;
        proxy_pass http://docker-frontend;
    }


}
